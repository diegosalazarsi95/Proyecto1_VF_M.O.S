`define NOP (ras && cas && we && ~cs)
`define Active (~ras && cas && we && ~cs)
`define Read (ras && ~cas && we && ~cs)
`define Write (ras && ~cas && ~we && ~cs)
`define Burst_terminate (ras && cas && ~we && ~cs)
`define Recharge (~ras && cas && ~we && ~cs)
`define Auto_refresh (~ras && ~cas && we && ~cs)
`define Load_mode_Reg (~ras && ~cas && ~we && ~cs)
